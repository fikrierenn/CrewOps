LAST: PostgreSQL, ilişkisel veritabanı olarak seçildi ve temel tablolar (Kullanıcılar, Toplantılar, Gündem Maddeleri, Notlar, Görevler) tasarlandı. | `MeetingParticipants` ara tablosu ile toplantı ve kullanıcılar arasında çoktan çoğa ilişki kuruldu. | Yabancı anahtarlar, veri bütünlüğü ve CASCADE/SET NULL stratejileri belirlendi. | Performans için gerekli indeksler tanımlandı (yabancı anahtarlar, e-posta, zaman damgaları, durum). | Prisma, şema yönetimi ve migration'lar için önerilen araçtır. | ```sql | PostgreSQL Schema Design | Enable UUID generation | CREATE EXTENSION IF NOT EXISTS "uuid-ossp"; | users Table | CREATE TABLE users ( | id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), | name VARCHAR(255) NOT NULL, | email VARCHAR(255) UNIQUE NOT NULL, | password_hash VARCHAR(255) NOT NULL, | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, | updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | meetings Table | CREATE TABLE meetings ( | id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), | title VARCHAR(255) NOT NULL, | description TEXT, | start_time TIMESTAMP WITH TIME ZONE NOT NULL, | end_time TIMESTAMP WITH TIME ZONE NOT NULL, | organizer_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, | updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | meeting_participants Table (Many-to-many relationship) | CREATE TABLE meeting_participants ( | meeting_id UUID NOT NULL REFERENCES meetings(id) ON DELETE CASCADE, | user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, | PRIMARY KEY (meeting_id, user_id), | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | agenda_items Table | CREATE TABLE agenda_items ( | id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), | meeting_id UUID NOT NULL REFERENCES meetings(id) ON DELETE CASCADE, | title VARCHAR(255) NOT NULL, | description TEXT, | item_order INTEGER NOT NULL, -- Renamed from 'order' to 'item_order' to avoid SQL keyword conflict | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, | updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | notes Table | CREATE TABLE notes ( | id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), | meeting_id UUID NOT NULL REFERENCES meetings(id) ON DELETE CASCADE, | user_id UUID REFERENCES users(id) ON DELETE SET NULL, -- If user is deleted, note remains but user_id becomes NULL | content TEXT NOT NULL, | ai_summary TEXT, -- Nullable, as AI processing might happen later or fail | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, | updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | tasks Table | CREATE TABLE tasks ( | id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), | meeting_id UUID NOT NULL REFERENCES meetings(id) ON DELETE CASCADE, | assigned_to_user_id UUID REFERENCES users(id) ON DELETE SET NULL, -- Task can exist even if assignee is deleted | description TEXT NOT NULL, | due_date TIMESTAMP WITH TIME ZONE, | status VARCHAR(50) NOT NULL DEFAULT 'pending', -- e.g., 'pending', 'in_progress', 'completed', 'cancelled' | created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, | updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP | ); | Indexes for performance | CREATE UNIQUE INDEX idx_users_email ON users (email); | CREATE INDEX idx_meetings_organizer_id ON meetings (organizer_id); | CREATE INDEX idx_meetings_start_time ON meetings (start_time); | CREATE INDEX idx_meeting_participants_meeting_id ON meeting_participants (meeting_id); | CREATE INDEX idx_meeting_participants_user_id ON meeting_participants (user_id); | CREATE INDEX idx_agenda_items_meeting_id ON agenda_items (meeting_id); | CREATE INDEX idx_notes_meeting_id ON notes (meeting_id); | CREATE INDEX idx_notes_user_id ON notes (user_id); | CREATE INDEX idx_tasks_meeting_id ON tasks (meeting_id); | CREATE INDEX idx_tasks_assigned_to_user_id ON tasks (assigned_to_user_id); | CREATE INDEX idx_tasks_due_date ON tasks (due_date); | CREATE INDEX idx_tasks_status ON tasks (status); | ```
NOW: Prisma ORM entegrasyonu başlatıldı ve veritabanı şeması tanımlandı. | Güvenli kullanıcı kaydı için temel API endpoint'i (`/api/users`) oluşturuldu. | Şifreler için `bcrypt` ile güvenli hashleme uygulandı. | Temel girdi doğrulama ve güvenli hata yönetimi eklendi. | Prisma client için singleton deseni uygulandı.
NEXT: `.env` dosyasındaki `DATABASE_URL` ortam değişkenini gerçek PostgreSQL bağlantı bilgilerinizle yapılandırın.
BLOCK: yok
CONTRACT_CHANGE?: no
